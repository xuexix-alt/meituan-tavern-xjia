---
description: 当用户提及 "MVU Zod"、"JSON Patch"、"Zod Schema" 或需要创建新版 MVU 变量系统时参考此规则
globs: ["**/*.md", "**/*.yaml", "**/*.js"]
---
# MVU Zod 进阶版工作流 (MVU Zod Workflow)

> **核心定义**：MVU Zod 是一个基于 Zod Schema 定义变量结构，并使用 JSON Patch 标准进行更新的进阶变量管理系统。它解决了经典版 MVU 的类型安全和边界控制问题。

## 适用场景
- 复杂的变量结构（如嵌套对象、RPG数值系统）
- 对类型安全有严格要求
- 希望减少 AI 更新变量时的幻觉和错误

## 核心工作流
构建 MVU Zod 系统需要依次完成以下 4 个步骤。请按照顺序执行。

### 步骤 1：变量结构设计 (Variable Structure)
使用 Zod 定义变量的 Schema。这决定了变量的类型、范围和默认值。

**Prompt 参考**：
"请根据项目需求，使用 Zod 4.x 定义变量结构的 Schema，并生成 `变量结构.js`。
要求：
- 使用 `import { registerMvuSchema } from '...'`。
- 数字类型使用 `z.coerce.number()` 并配合 `.transform(v => _.clamp(v, min, max))` 进行边界控制。
- 优先使用 `z.record()` 而非数组。
- 确保幂等性：`Schema.parse(input) === Schema.parse(Schema.parse(input))`。"

**输出文件**：`src/.../变量结构.js`

### 步骤 2：变量初始化 (Initialization)
基于 Schema 生成初始值 YAML。

**Prompt 参考**：
"请读取 `变量结构.js`，基于其中的 Schema 定义，生成符合类型的初始值 YAML 数据块。
要求：
- 使用 YAML 格式。
- 仅包含变量名和初始值。
- 所有值必须符合 Schema 类型。"

**输出文件**：`src/.../[initvar]变量初始化.yaml`

### 步骤 3：变量更新规则 (Update Rules)
定义变量何时更新、如何更新。

**Prompt 参考**：
"请读取 `变量结构.js`，生成变量更新规则 YAML。
格式：
```yaml
变量名:
  check:
    - 何时更新
    - 如何更新
```
要求：
- 省略已在 Schema 中定义的类型和范围限制。
- 合并相似规则（如 `${六维}`）。"

**输出文件**：`src/.../[mvu_update]变量更新规则.yaml`

### 步骤 4：变量输出格式 (Output Format)
定义 AI 输出 JSON Patch 的格式。

**Prompt 参考**：
"请生成 MVU Zod 标准的输出格式指令。
要求：
- 使用 JSON Patch (RFC 6902) 标准。
- 包含 `<update_analysis>` 和 `<json_patch>` 块。
- 路径使用 JSON Pointer (`/path/to/variable`)。
- 操作仅限 `replace`, `add`, `remove`。"

**输出文件**：`src/.../[mvu_update]变量输出格式.yaml`

## 常见问题
- **数组 vs 对象**：推荐使用对象 (`z.record`)，因为 JSON Patch 对对象的路径支持更好，且不易因顺序变化而出错。
- **类型转换**：务必使用 `z.coerce.number()` 处理 LLM 可能输出字符串数字的情况。
- **边界控制**：务必使用 `.transform(v => _.clamp(v, 0, 100))` 强制数值范围，防止数值溢出。
